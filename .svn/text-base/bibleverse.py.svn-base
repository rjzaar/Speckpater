# This program gives a set of verses.
import random




TOTALVERSES=100 # Total number of bible verses
NUMVERSES=20    #number of bible verses to use each game.
class bibleverse:
	def __init__(self):
		#load all verses into the file of verses
		f = open('bible.txt', 'w')
		self.verses = []
		for i in range(5):
			self.verses.append(f.readline())
		f.close()
		
		


	def getverse(self):
		return (self.verses[int(random.random()*5)])
	
		

